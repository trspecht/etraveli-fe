import { Container, InnerContainer, Input } from "./styles"

export const SearchBar = () => {

  return (
    <Container>
      <InnerContainer>
        <Input />
      </InnerContainer>
    </Container>
  )
}